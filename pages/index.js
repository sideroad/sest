// @flow
// TODO: Generate HTML to confirm differences
// import React from "react";
//
// const Page = ({ items = [] }) => (
//   <div>{items.map(item => <div key={item.name}>{item.name}</div>)}</div>
// );
//
// Page.getInitialProps = async ({ req }) => {
//   // const res = await fetch("https://api.github.com/repos/zeit/next.js");
//   // const json = await res.json();
//   // TODO: fetch from API
//   return {
//     items: [
//       { name: "sideroad", url: "http://sideroad.secret.jp", ratio: 50 },
//       { name: "Google", url: "https://google.com", ratio: 5 }
//     ]
//   };
// };
//
// export default Page;
